"""Shared test fixtures.

Add the following as the project grows:
- Test database session (using a separate test DB or transactional rollback)
- FastAPI TestClient configured with the app
- Factory functions for creating test users, communities, etc.
- Authenticated client helpers (pre-set JWT headers)
"""
